name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  strict-rules:
    name: Forbidden keyword scan (no new/delete/malloc)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: scan first-party sources
        run: |
          set -e
          HITS=$(grep -rnwE 'new|delete|malloc|free|calloc|realloc' \
                   server client common tools tests \
                   --include='*.c' --include='*.cc' --include='*.cpp' --include='*.cxx' --include='*.h' --include='*.hpp' \
                 | grep -vE '= delete;' \
                 | grep -vE 'freeaddrinfo' || true)
          if [ -n "$HITS" ]; then
            echo "FORBIDDEN MANUAL MEMORY MANAGEMENT FOUND:"
            echo "$HITS"
            exit 1
          fi
          echo "clean: no manual memory management keywords"

  linux:
    name: Linux build + unit + E2E tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Release build
        run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
          cmake --build build -j"$(nproc)"
      - name: ctest (unit + E2E robustness)
        run: cd build && ctest --output-on-failure

  linux-asan:
    name: AddressSanitizer + LeakSanitizer
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: ASan build
        run: |
          cmake -S . -B build-asan -DENABLE_ASAN=ON
          cmake --build build-asan -j"$(nproc)"
      - name: ctest under sanitizers (leak == test failure)
        env:
          ASAN_OPTIONS: detect_leaks=1:halt_on_error=1
          UBSAN_OPTIONS: halt_on_error=1:print_stacktrace=1
        run: cd build-asan && ctest --output-on-failure

  linux-tsan:
    name: ThreadSanitizer concurrency tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: TSan build
        run: |
          cmake -S . -B build-tsan -DENABLE_TSAN=ON
          cmake --build build-tsan -j"$(nproc)"
      - name: ctest under ThreadSanitizer
        env:
          TSAN_OPTIONS: halt_on_error=1
        run: cd build-tsan && ctest --output-on-failure

  windows-cross:
    name: Windows GUI + CLI clients (zig cross-compile)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: install zig
        run: pip install ziglang
      - name: cross-compile log_client_gui.exe
        run: ./scripts/build_windows_client.sh
      - uses: actions/upload-artifact@v4
        with:
          name: log-transfer-clients-windows-x64
          path: |
            dist/windows/log_client_gui.exe
            dist/windows/log_client_cli.exe
